node_modules/
.next/
tsconfig.tsbuildinfo

.vercel

# Local env files — never commit these, they hold live Supabase credentials
.env
.env*.local

# Next.js generated types
next-env.d.ts

# Supabase CLI cache (created by ad-hoc `npx supabase` probes, not project config)
supabase/.temp/
